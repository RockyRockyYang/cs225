#include "binarytree.h"

BinaryTree::Node* BinaryTree::findClosest(int value) const {
    /* Your code here! */
	if(root == NULL)
    		return NULL;
	else 
		return findClosest(root, value);
}

BinaryTree::Node* BinaryTree::findClosest(Node* subRoot, int value) const {
	return NULL;
}
